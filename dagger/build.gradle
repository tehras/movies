apply plugin: 'com.android.library'
apply from: '../gradle/common-android.gradle'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])

    implementation deps.appCompat
    api deps.dagger

    kapt deps.daggerApt
}
